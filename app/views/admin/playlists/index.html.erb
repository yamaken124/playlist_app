<h2>プレイリスト一覧</h2>
<div class="container-fluid">
  <div class="row">
    <table class="table table-striped table-hover ">
      <thead>
        <tr>
          <th>ユーザー名</th>
          <th>プレイリスト名</th>
          <th>お気に入り数</th>
          <th>更新日時</th>
        </tr>
      </thead>
      <% i = 0 %>
      <% @playlists.each do |playlist| %>
        <tbody>
          <tr>
            <td>
              <%= link_to @user_name_array[i], admin_user_path(id: @playlist_user_ids[i]) %>
              </td>
            <% i += 1 %>
            <td>
              <%= link_to playlist.title, admin_playlist_path(id: playlist.id) %>
              </td>
            <td>
                <%= playlist.favorites.count %>
              </td>
            <td>
              <% update_time = playlist.updated_at %>
              <%= update_time.strftime("%Y-%m-%d %H:%M:%S") %>
              </td>
              <td>
              <%= link_to "プレイリスト削除", admin_playlist_path(id: playlist.id), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-danger" %>
              </td>
          </tr>
        </tbody>
      <% end %>
    </table>
  </div>
</div>
